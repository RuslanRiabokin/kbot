# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "ruslanriabokin"
  tag: "v1.0.5-68fc0c6"
  os: linux
  arch: amd64
secret:
  name: kbot
  key: token
  env: TELE_TOKEN
